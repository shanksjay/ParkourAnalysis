# Core dependencies for Parkour Analysis
# Install with: pip install -r requirements.txt
# Or use UV: uv pip install -r requirements.txt

# Computer Vision
opencv-python>=4.8.0,<5.0.0

# Deep Learning
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0

# Scientific Computing
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<2.0.0

# Visualization
matplotlib>=3.7.0,<4.0.0

# Person Tracking and Segmentation
# Note: SAM3 should be installed from source: git clone https://github.com/facebookresearch/sam3.git && cd sam3 && pip install -e .
# ultralytics>=8.0.0,<9.0.0  # Optional: YOLO (replaced by SAM3)

# 3D Body Modeling
smplx>=0.1.28,<1.0.0
chumpy>=0.70,<1.0.0  # Required by smplx

# 3D Mesh Processing
trimesh>=3.20.0,<4.0.0
pyrender>=0.1.45,<1.0.0

# PDF Generation
reportlab>=4.0.0,<5.0.0

# Hugging Face Hub (required for SAM3 model downloads)
huggingface-hub>=0.19.0,<1.0.0

# Optional: For better tracking (mentioned in code comments)
# lapx>=0.1.0  # Uncomment if using ByteTrack

